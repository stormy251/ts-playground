{
  "tasks": {
    "start": "deno run --allow-net index.ts",
    "dev": "deno run --watch --allow-net index.ts",
    "test": "deno test",
    "lint": "deno lint",
    "fmt": "deno fmt"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 80,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "compilerOptions": {
    "lib": ["deno.window"]
  }
}
